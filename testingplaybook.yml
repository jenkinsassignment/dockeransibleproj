- hosts: docker-slave
  tasks:
   - name: Hi. I ensure connecting to the Docker machine
     ping:
   - name: Create slave Docker bibinwilson 
     docker_container:
      docker_host: "tcp://192.168.2.204:4243"
      image: bibinwilson/jenkins-slave
      state: started
      name: dockerslave
      command: "java -jar jenkins-cli.jar -s something build builds/$BUILD_NUMBER --username jenkins --password jenkins"